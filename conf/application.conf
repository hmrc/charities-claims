# Copyright 2025 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include "backend.conf"

# This is the main configuration file for the application.
# ~~~~~

appName = charities-claims

# Default http client
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientV2Module"
play.modules.enabled += "uk.gov.hmrc.mongo.play.PlayMongoModule"
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.AuthModule"

# Play Modules
play.modules.enabled += "uk.gov.hmrc.charitiesclaims.Module"

play.http.errorHandler = "play.api.http.JsonHttpErrorHandler"


# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# !!!WARNING!!! DO NOT CHANGE THIS ROUTER
play.http.router = prod.Routes

# Controller
# ~~~~~
# By default all controllers will have authorisation, logging and
# auditing (transaction monitoring) enabled.
# The below controllers are the default exceptions to this rule.

controllers {
}

# Microservice specific config

microservice {
  services {
    auth {
      protocol = http
      host = localhost
      port = 8500
    }
    charities-claims-validation {
      protocol = http
      host = localhost
      port = 8032
      context-path = "/charities-claims-validation"
      retryIntervals = [500ms, 1s]
    }
    formp-proxy {
      protocol = http
      host = localhost
      port = 6995
      context-path = "/formp-proxy"
      retryIntervals = [500ms, 1s]
    }
    chris {
      baseUrl = "http://localhost:9712/submission"
      path = "/ChRIS/Charities/Filing/sync/HMRC-CHAR-CLM"
      retryIntervals = [1s, 2s, 3s]
    }
  }
}

metrics {
  enabled = true
}

mongo-async-driver {
  akka {
    log-dead-letters-during-shutdown = off
    log-dead-letters = 0
  }
}

mongodb {
  uri = "mongodb://localhost:27017/charities-claims"
  ttl = 31 days
}

agentUnsubmittedClaimLimit = 120
computeLiteIRMark = true # If true, the IR mark will be computed using the lite IR mark algorithm with reduced canonicalisation steps
